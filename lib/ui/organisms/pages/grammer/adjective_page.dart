import 'package:flutter/material.dart';

class AdjectivePage extends StatelessWidget {
  const AdjectivePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Adjective')),
      body: Center(child: Text('Adjective Page')),
    );
  }
}
