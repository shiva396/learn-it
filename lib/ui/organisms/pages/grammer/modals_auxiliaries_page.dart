import 'package:flutter/material.dart';

class ModalsAuxiliariesPage extends StatelessWidget {
  const ModalsAuxiliariesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Modals and Modal Auxiliaries')),
      body: Center(child: Text('Modals and Modal Auxiliaries Page')),
    );
  }
}
