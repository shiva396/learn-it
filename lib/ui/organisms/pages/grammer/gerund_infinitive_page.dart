import 'package:flutter/material.dart';

class GerundInfinitivePage extends StatelessWidget {
  const GerundInfinitivePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gerund and Infinitive')),
      body: Center(child: Text('Gerund and Infinitive Page')),
    );
  }
}
